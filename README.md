# EECE7205-Final-Project

Data explorer and preprocessor are both used to extract and normalize the data used to train the models

Each training module is included and runs on pytorch

Benchmark_compare is used to compare models
